<div>
    <h1>Function <span class="referenceLink function">{{name}}</span></h1>

    {{#each signatures}}
    <div>
        ({{{join parameters ", "}}}) => {{{returnType}}}
    </div>
    {{/each}}
</div>