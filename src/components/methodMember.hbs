
<div id={{name}}>
    {{#if isStatic}}[static]{{/if}}
    {{#if isProtected}}[protected]{{/if}}
    {{#if isAbstract}}[abstract]{{/if}}
    {{#if isPrivate}}[private]{{/if}}
    <div class="docblock">
        {{{comment}}}
    </div>
    {{#each signatures}}
    <p>
        <span class="item-name">{{../name}}</span>{{~#if typeParameters}} <span>&lt;{{{join typeParameters ", "}}}&gt;</span> {{/if~}}<span>({{~{join parameters ", "}~}})</span>{{~#if returnType}} <span>=> {{{returnType}}}</span> {{/if~}}
    </p>
    {{/each}}
</div>