<div>
    <h1>Enum <span class="referenceLink object">{{name}}</span>
    
    {{#each loc}}
        <a href={{this.sourceFile}}>{{> sourceCode}}</a>
    {{/each}}
    </h1>

    {{{comment}}}

    {{#if members}}
    <h2>Members</h2>
    {{#each members}}
    <div id="enum.{{this.name}}">
        <span class="item-name">{{this.name}}</span>

        {{#if this.initializer}}
        <span>= {{{this.initializer}}}</span>
        {{/if}}

    </div>
    {{/each}}
    {{/if}}


</div>